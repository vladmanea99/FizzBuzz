import ICommand from './ICommand'

class Invoker{
    commands: Array<ICommand> ;
    constructor(file: string){
        /*
        read data from file and create the commands

        for (i = 0; i < nOfCommands; i++){
            commands[i] = getTypeOfCommand(typeOfCommand)
        }
         */
    }
}